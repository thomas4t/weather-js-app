export const theme = {
  table: {
    disable: true,
  },
}

export const user = {
  table: {
    disable: true,
  },
}

export const as = {
  table: {
    type: { summary: ['string|React.Component'] },
  },
  options: ['a'],
  control: {
    type: 'select',
  },
}
